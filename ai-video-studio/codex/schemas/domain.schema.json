{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gmpark-creator.github.io/project-dashboard/ai-video-studio/codex/schemas/domain.schema.json",
  "title": "AI Video Studio Domain Schema",
  "type": "object",
  "$defs": {
    "Intent": {
      "type": "string",
      "enum": ["shorts", "product_ad", "app_intro", "real_estate", "education", "brand"]
    },
    "Tier": {
      "type": "string",
      "enum": ["fast", "economy", "final"]
    },
    "Aspect": {
      "type": "string",
      "enum": ["9:16", "16:9", "1:1", "4:5"]
    },
    "ProjectStatus": {
      "type": "string",
      "enum": ["draft", "storyboarded", "generating", "reviewing", "edited", "rendering", "done", "failed"]
    },
    "ShotStatus": {
      "type": "string",
      "enum": ["pending", "generating", "reviewing", "selected", "failed"]
    },
    "JobStatus": {
      "type": "string",
      "enum": ["queued", "running", "done", "failed", "cancelled"]
    },
    "AssetKind": {
      "type": "string",
      "enum": ["image", "video", "audio", "brand"]
    },
    "RenderSourceHash": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$"
    },
    "AssetUsageMode": {
      "type": "string",
      "enum": ["first_frame", "last_frame", "style_reference", "character_reference", "product_reference", "background_reference"]
    },
    "ImageAssetRole": {
      "type": "string",
      "enum": ["product", "character", "location", "style", "keyframe", "thumbnail", "logo", "background"]
    },
    "ImageMakerPurpose": {
      "type": "string",
      "enum": ["photoreal", "product", "character", "background", "style", "poster", "thumbnail", "transparent"]
    },
    "SAEC": {
      "type": "object",
      "required": ["subject", "action", "environment", "camera", "framing", "lighting", "style", "negative"],
      "additionalProperties": false,
      "properties": {
        "subject": { "type": "string" },
        "action": { "type": "string" },
        "environment": { "type": "string" },
        "camera": { "type": "string" },
        "framing": { "type": "string" },
        "lighting": { "type": "string" },
        "style": { "type": "string" },
        "negative": { "type": "string" }
      }
    },
    "ShotRequirements": {
      "type": "object",
      "required": ["tier", "aspect", "imageToVideo", "needsLipsyncAudio", "motionHeavy", "characterLock"],
      "additionalProperties": false,
      "properties": {
        "tier": { "$ref": "#/$defs/Tier" },
        "aspect": { "$ref": "#/$defs/Aspect" },
        "resolution": { "type": "string", "enum": ["540p", "720p", "1080p", "4k"] },
        "imageToVideo": { "type": "boolean" },
        "needsLipsyncAudio": { "type": "boolean" },
        "motionHeavy": { "type": "boolean" },
        "characterLock": { "type": "boolean" },
        "characterId": { "type": ["string", "null"] },
        "region": { "type": "string" }
      }
    },
    "QualityFlag": {
      "type": "object",
      "required": ["axis", "score", "hint"],
      "additionalProperties": false,
      "properties": {
        "axis": {
          "type": "string",
          "enum": ["fidelity", "consistency", "motion", "transition", "audio", "completeness"]
        },
        "score": { "type": "number", "minimum": 1, "maximum": 5 },
        "hint": { "type": "string" }
      }
    },
    "Project": {
      "type": "object",
      "required": ["id", "title", "intent", "status", "aspect", "targetDurationSec", "progress", "defaultRenderJobId", "createdAt", "updatedAt"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^prj_" },
        "title": { "type": "string" },
        "intent": { "$ref": "#/$defs/Intent" },
        "status": { "$ref": "#/$defs/ProjectStatus" },
        "aspect": { "$ref": "#/$defs/Aspect" },
        "targetDurationSec": { "type": "integer", "minimum": 1 },
        "progress": {
          "type": "object",
          "required": ["shotsDone", "shotsTotal"],
          "additionalProperties": false,
          "properties": {
            "shotsDone": { "type": "integer", "minimum": 0 },
            "shotsTotal": { "type": "integer", "minimum": 0 }
          }
        },
        "characters": {
          "type": "array",
          "items": { "$ref": "#/$defs/Character" },
          "default": []
        },
        "thumbUrl": { "type": ["string", "null"], "format": "uri" },
        "defaultRenderJobId": { "type": ["string", "null"], "pattern": "^rnd_" },
        "credits": { "$ref": "#/$defs/CreditSummary" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      }
    },
    "Character": {
      "type": "object",
      "required": ["id", "label", "refImageUrls"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^chr_" },
        "label": { "type": "string" },
        "refImageUrls": {
          "type": "array",
          "items": { "type": "string", "format": "uri" }
        }
      }
    },
    "CreditSummary": {
      "type": "object",
      "required": ["spent", "estimateRemaining"],
      "additionalProperties": false,
      "properties": {
        "spent": { "type": "number", "minimum": 0 },
        "estimateRemaining": { "type": "number", "minimum": 0 }
      }
    },
    "StudioCredits": {
      "type": "object",
      "required": ["balance", "spent", "reserved"],
      "additionalProperties": false,
      "properties": {
        "balance": { "type": "number", "minimum": 0 },
        "spent": { "type": "number", "minimum": 0 },
        "reserved": { "type": "number", "minimum": 0 }
      }
    },
    "CreditTransaction": {
      "type": "object",
      "required": ["id", "projectId", "jobId", "kind", "action", "credits", "balanceAfter", "note", "createdAt"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^ctx_" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "jobId": { "type": ["string", "null"] },
        "kind": { "type": "string", "enum": ["reserve", "capture", "refund"] },
        "action": { "type": "string", "enum": ["generateImages", "generateShot", "upgradeTake", "startRender"] },
        "credits": { "type": "number", "minimum": 0 },
        "balanceAfter": {
          "type": "object",
          "required": ["spent", "reserved", "available"],
          "additionalProperties": false,
          "properties": {
            "spent": { "type": "number", "minimum": 0 },
            "reserved": { "type": "number", "minimum": 0 },
            "available": { "type": "number", "minimum": 0 }
          }
        },
        "note": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "MediaArtifact": {
      "type": "object",
      "required": ["id", "projectId", "ownerType", "ownerId", "sourceJobId", "kind", "role", "url", "storageKey", "contentType", "bytes", "status", "createdAt"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^art_" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "ownerType": { "type": "string", "enum": ["imageAsset", "take", "renderJob"] },
        "ownerId": { "type": "string" },
        "sourceJobId": { "type": ["string", "null"] },
        "kind": { "$ref": "#/$defs/AssetKind" },
        "role": { "type": "string", "enum": ["image_asset", "image_thumbnail", "take_video", "take_poster", "render_output"] },
        "url": { "type": "string" },
        "storageKey": { "type": "string" },
        "contentType": { "type": "string" },
        "bytes": { "type": ["number", "null"], "minimum": 0 },
        "status": { "type": "string", "enum": ["stored", "external"] },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "MediaArtifactCleanup": {
      "type": "string",
      "enum": ["retain", "review_external", "orphaned"]
    },
    "MediaArtifactInventoryItem": {
      "type": "object",
      "required": ["artifact", "projectTitle", "ownerExists", "referenced", "referenceCount", "cleanup"],
      "additionalProperties": false,
      "properties": {
        "artifact": { "$ref": "#/$defs/MediaArtifact" },
        "projectTitle": { "type": "string" },
        "ownerExists": { "type": "boolean" },
        "referenced": { "type": "boolean" },
        "referenceCount": { "type": "integer", "minimum": 0 },
        "cleanup": { "$ref": "#/$defs/MediaArtifactCleanup" }
      }
    },
    "MediaArtifactInventory": {
      "type": "object",
      "required": ["generatedAt", "summary", "artifacts"],
      "additionalProperties": false,
      "properties": {
        "generatedAt": { "type": "string", "format": "date-time" },
        "summary": {
          "type": "object",
          "required": ["total", "stored", "external", "images", "videos", "knownBytes", "unknownBytes", "orphaned", "reviewExternal"],
          "additionalProperties": false,
          "properties": {
            "total": { "type": "integer", "minimum": 0 },
            "stored": { "type": "integer", "minimum": 0 },
            "external": { "type": "integer", "minimum": 0 },
            "images": { "type": "integer", "minimum": 0 },
            "videos": { "type": "integer", "minimum": 0 },
            "knownBytes": { "type": "number", "minimum": 0 },
            "unknownBytes": { "type": "integer", "minimum": 0 },
            "orphaned": { "type": "integer", "minimum": 0 },
            "reviewExternal": { "type": "integer", "minimum": 0 }
          }
        },
        "artifacts": { "type": "array", "items": { "$ref": "#/$defs/MediaArtifactInventoryItem" } }
      }
    },
    "StorageCleanupAction": {
      "type": "string",
      "enum": ["retain", "review_external", "delete_object"]
    },
    "StorageCleanupPlanItem": {
      "type": "object",
      "required": ["artifact", "cleanup", "action", "storageKey", "ownerExists", "referenced", "referenceCount", "safeToDelete", "reason"],
      "additionalProperties": false,
      "properties": {
        "artifact": { "$ref": "#/$defs/MediaArtifact" },
        "cleanup": { "$ref": "#/$defs/MediaArtifactCleanup" },
        "action": { "$ref": "#/$defs/StorageCleanupAction" },
        "storageKey": { "type": "string" },
        "ownerExists": { "type": "boolean" },
        "referenced": { "type": "boolean" },
        "referenceCount": { "type": "integer", "minimum": 0 },
        "safeToDelete": { "type": "boolean" },
        "reason": { "type": "string" }
      }
    },
    "StorageCleanupPlan": {
      "type": "object",
      "required": ["generatedAt", "summary", "items"],
      "additionalProperties": false,
      "properties": {
        "generatedAt": { "type": "string", "format": "date-time" },
        "summary": {
          "type": "object",
          "required": ["total", "retain", "reviewExternal", "deleteCandidates", "knownReclaimableBytes", "unknownReclaimableItems"],
          "additionalProperties": false,
          "properties": {
            "total": { "type": "integer", "minimum": 0 },
            "retain": { "type": "integer", "minimum": 0 },
            "reviewExternal": { "type": "integer", "minimum": 0 },
            "deleteCandidates": { "type": "integer", "minimum": 0 },
            "knownReclaimableBytes": { "type": "number", "minimum": 0 },
            "unknownReclaimableItems": { "type": "integer", "minimum": 0 }
          }
        },
        "items": { "type": "array", "items": { "$ref": "#/$defs/StorageCleanupPlanItem" } }
      }
    },
    "CancelJobResult": {
      "type": "object",
      "required": ["jobId", "kind", "projectId", "cancelled", "status", "refundedCredits", "reason"],
      "additionalProperties": false,
      "properties": {
        "jobId": { "type": "string" },
        "kind": { "type": ["string", "null"], "enum": ["generationJob", "renderJob", "imageJob", null] },
        "projectId": { "type": ["string", "null"] },
        "cancelled": { "type": "boolean" },
        "status": {
          "anyOf": [
            { "$ref": "#/$defs/JobStatus" },
            { "type": "null" }
          ]
        },
        "refundedCredits": { "type": "number", "minimum": 0 },
        "reason": { "type": "string" }
      }
    },
    "QueueJobKind": {
      "type": "string",
      "enum": ["generation", "image", "render"]
    },
    "QueueJobSnapshot": {
      "type": "object",
      "required": ["id", "projectId", "kind", "status", "stage", "progress", "etaSec", "queuedAt", "updatedAt", "dueAt", "cancelable"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "kind": { "$ref": "#/$defs/QueueJobKind" },
        "status": { "$ref": "#/$defs/JobStatus" },
        "stage": { "type": "string" },
        "progress": { "type": "number", "minimum": 0, "maximum": 1 },
        "etaSec": { "type": ["integer", "null"], "minimum": 0 },
        "queuedAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "dueAt": { "type": "integer" },
        "cancelable": { "type": "boolean" }
      }
    },
    "JobQueueSnapshot": {
      "type": "object",
      "required": ["generatedAt", "summary", "jobs"],
      "additionalProperties": false,
      "properties": {
        "generatedAt": { "type": "string", "format": "date-time" },
        "summary": {
          "type": "object",
          "required": ["total", "queued", "running", "done", "failed", "cancelled", "active", "overdue", "cancelable", "nextDueAt"],
          "additionalProperties": false,
          "properties": {
            "total": { "type": "integer", "minimum": 0 },
            "queued": { "type": "integer", "minimum": 0 },
            "running": { "type": "integer", "minimum": 0 },
            "done": { "type": "integer", "minimum": 0 },
            "failed": { "type": "integer", "minimum": 0 },
            "cancelled": { "type": "integer", "minimum": 0 },
            "active": { "type": "integer", "minimum": 0 },
            "overdue": { "type": "integer", "minimum": 0 },
            "cancelable": { "type": "integer", "minimum": 0 },
            "nextDueAt": { "type": ["integer", "null"] }
          }
        },
        "jobs": { "type": "array", "items": { "$ref": "#/$defs/QueueJobSnapshot" } }
      }
    },
    "WorkerDispatchKind": {
      "type": "string",
      "enum": ["provider_generation", "image_generation", "render"]
    },
    "WorkerDispatchItem": {
      "type": "object",
      "required": ["dispatchKey", "kind", "jobId", "projectId", "status", "stage", "etaSec", "queuedAt", "updatedAt", "dueAt", "priority", "cancelable", "invocation"],
      "additionalProperties": false,
      "properties": {
        "dispatchKey": { "type": "string" },
        "kind": { "$ref": "#/$defs/WorkerDispatchKind" },
        "jobId": { "type": "string", "pattern": "^(gen_|ijob_|rnd_)" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "status": { "type": "string", "enum": ["queued", "running"] },
        "stage": { "type": "string" },
        "etaSec": { "type": ["integer", "null"], "minimum": 0 },
        "queuedAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "dueAt": { "type": "integer" },
        "priority": { "type": "integer", "minimum": 1 },
        "cancelable": { "type": "boolean" },
        "invocation": {
          "oneOf": [
            { "$ref": "#/$defs/ProviderInvocation" },
            { "$ref": "#/$defs/ImageWorkerInvocation" },
            { "$ref": "#/$defs/RenderWorkerInvocation" }
          ]
        }
      }
    },
    "WorkerDispatchSnapshot": {
      "type": "object",
      "required": ["generatedAt", "summary", "items"],
      "additionalProperties": false,
      "properties": {
        "generatedAt": { "type": "string", "format": "date-time" },
        "summary": {
          "type": "object",
          "required": ["total", "providerGeneration", "imageGeneration", "render", "queued", "running", "overdue", "nextDueAt"],
          "additionalProperties": false,
          "properties": {
            "total": { "type": "integer", "minimum": 0 },
            "providerGeneration": { "type": "integer", "minimum": 0 },
            "imageGeneration": { "type": "integer", "minimum": 0 },
            "render": { "type": "integer", "minimum": 0 },
            "queued": { "type": "integer", "minimum": 0 },
            "running": { "type": "integer", "minimum": 0 },
            "overdue": { "type": "integer", "minimum": 0 },
            "nextDueAt": { "type": ["integer", "null"] }
          }
        },
        "items": { "type": "array", "items": { "$ref": "#/$defs/WorkerDispatchItem" } }
      }
    },
    "WorkerLeaseStatus": {
      "type": "string",
      "enum": ["active", "released", "expired"]
    },
    "WorkerLease": {
      "type": "object",
      "required": ["id", "token", "dispatchKey", "kind", "jobId", "projectId", "workerId", "status", "leasedAt", "expiresAt", "releasedAt"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^wlease_" },
        "token": { "type": "string" },
        "dispatchKey": { "type": "string" },
        "kind": { "$ref": "#/$defs/WorkerDispatchKind" },
        "jobId": { "type": "string", "pattern": "^(gen_|ijob_|rnd_)" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "workerId": { "type": "string" },
        "status": { "$ref": "#/$defs/WorkerLeaseStatus" },
        "leasedAt": { "type": "string", "format": "date-time" },
        "expiresAt": { "type": "string", "format": "date-time" },
        "releasedAt": { "type": ["string", "null"], "format": "date-time" }
      }
    },
    "WorkerLeaseRequest": {
      "type": "object",
      "required": ["workerId"],
      "additionalProperties": false,
      "properties": {
        "workerId": { "type": "string" },
        "kind": {
          "anyOf": [
            { "$ref": "#/$defs/WorkerDispatchKind" },
            { "type": "string", "const": "any" }
          ]
        },
        "ttlSec": { "type": "integer", "minimum": 5, "maximum": 600 }
      }
    },
    "WorkerLeaseResult": {
      "type": "object",
      "required": ["lease", "item", "reason"],
      "additionalProperties": false,
      "properties": {
        "lease": {
          "anyOf": [
            { "$ref": "#/$defs/WorkerLease" },
            { "type": "null" }
          ]
        },
        "item": {
          "anyOf": [
            { "$ref": "#/$defs/WorkerDispatchItem" },
            { "type": "null" }
          ]
        },
        "reason": { "type": "string", "enum": ["leased", "no_available_work"] }
      }
    },
    "WorkerLeaseReleaseResult": {
      "type": "object",
      "required": ["leaseId", "released", "status", "reason"],
      "additionalProperties": false,
      "properties": {
        "leaseId": { "type": "string", "pattern": "^wlease_" },
        "released": { "type": "boolean" },
        "status": {
          "anyOf": [
            { "$ref": "#/$defs/WorkerLeaseStatus" },
            { "type": "null" }
          ]
        },
        "reason": { "type": "string", "enum": ["released", "not_found", "token_mismatch", "not_active"] }
      }
    },
    "WorkerLeaseRenewResult": {
      "type": "object",
      "required": ["leaseId", "renewed", "lease", "status", "reason"],
      "additionalProperties": false,
      "properties": {
        "leaseId": { "type": "string", "pattern": "^wlease_" },
        "renewed": { "type": "boolean" },
        "lease": {
          "anyOf": [
            { "$ref": "#/$defs/WorkerLease" },
            { "type": "null" }
          ]
        },
        "status": {
          "anyOf": [
            { "$ref": "#/$defs/WorkerLeaseStatus" },
            { "type": "null" }
          ]
        },
        "reason": { "type": "string", "enum": ["renewed", "not_found", "token_mismatch", "not_active"] }
      }
    },
    "WorkerLeaseSnapshot": {
      "type": "object",
      "required": ["generatedAt", "summary", "leases"],
      "additionalProperties": false,
      "properties": {
        "generatedAt": { "type": "string", "format": "date-time" },
        "summary": {
          "type": "object",
          "required": ["total", "active", "released", "expired"],
          "additionalProperties": false,
          "properties": {
            "total": { "type": "integer", "minimum": 0 },
            "active": { "type": "integer", "minimum": 0 },
            "released": { "type": "integer", "minimum": 0 },
            "expired": { "type": "integer", "minimum": 0 }
          }
        },
        "leases": { "type": "array", "items": { "$ref": "#/$defs/WorkerLease" } }
      }
    },
    "WorkerCompletionStatus": {
      "type": "string",
      "enum": ["succeeded", "failed", "cancelled"]
    },
    "WorkerCompletionReceipt": {
      "type": "object",
      "required": ["completionKey", "kind", "jobId", "projectId", "status", "completedAt", "error", "artifacts", "creditTransactions", "summary"],
      "additionalProperties": false,
      "properties": {
        "completionKey": { "type": "string" },
        "kind": { "$ref": "#/$defs/WorkerDispatchKind" },
        "jobId": { "type": "string", "pattern": "^(gen_|ijob_|rnd_)" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "status": { "$ref": "#/$defs/WorkerCompletionStatus" },
        "completedAt": { "type": "string", "format": "date-time" },
        "error": { "$ref": "#/$defs/ErrorResponse" },
        "artifacts": { "type": "array", "items": { "$ref": "#/$defs/MediaArtifact" } },
        "creditTransactions": { "type": "array", "items": { "$ref": "#/$defs/CreditTransaction" } },
        "summary": {
          "type": "object",
          "required": ["artifactCount", "storedArtifacts", "externalArtifacts", "capturedCredits", "refundedCredits"],
          "additionalProperties": false,
          "properties": {
            "artifactCount": { "type": "integer", "minimum": 0 },
            "storedArtifacts": { "type": "integer", "minimum": 0 },
            "externalArtifacts": { "type": "integer", "minimum": 0 },
            "capturedCredits": { "type": "number", "minimum": 0 },
            "refundedCredits": { "type": "number", "minimum": 0 }
          }
        }
      }
    },
    "WorkerCompletionSnapshot": {
      "type": "object",
      "required": ["generatedAt", "summary", "receipts"],
      "additionalProperties": false,
      "properties": {
        "generatedAt": { "type": "string", "format": "date-time" },
        "summary": {
          "type": "object",
          "required": ["total", "succeeded", "failed", "cancelled", "artifactCount", "capturedCredits", "refundedCredits"],
          "additionalProperties": false,
          "properties": {
            "total": { "type": "integer", "minimum": 0 },
            "succeeded": { "type": "integer", "minimum": 0 },
            "failed": { "type": "integer", "minimum": 0 },
            "cancelled": { "type": "integer", "minimum": 0 },
            "artifactCount": { "type": "integer", "minimum": 0 },
            "capturedCredits": { "type": "number", "minimum": 0 },
            "refundedCredits": { "type": "number", "minimum": 0 }
          }
        },
        "receipts": { "type": "array", "items": { "$ref": "#/$defs/WorkerCompletionReceipt" } }
      }
    },
    "RuntimeReadiness": {
      "type": "object",
      "required": ["mode", "generatedAt", "ready", "missingEnv", "checks"],
      "additionalProperties": false,
      "properties": {
        "mode": { "type": "string", "enum": ["mock", "production"] },
        "generatedAt": { "type": "string", "format": "date-time" },
        "ready": { "type": "boolean" },
        "missingEnv": { "type": "array", "items": { "type": "string" } },
        "checks": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["id", "label", "status", "detail"],
            "additionalProperties": false,
            "properties": {
              "id": { "type": "string" },
              "label": { "type": "string" },
              "status": { "type": "string", "enum": ["pass", "warn", "fail"] },
              "detail": { "type": "string" }
            }
          }
        }
      }
    },
    "JobStatusCounts": {
      "type": "object",
      "required": ["queued", "running", "done", "failed", "cancelled"],
      "additionalProperties": false,
      "properties": {
        "queued": { "type": "integer", "minimum": 0 },
        "running": { "type": "integer", "minimum": 0 },
        "done": { "type": "integer", "minimum": 0 },
        "failed": { "type": "integer", "minimum": 0 },
        "cancelled": { "type": "integer", "minimum": 0 }
      }
    },
    "SystemMetrics": {
      "type": "object",
      "required": ["generatedAt", "projects", "jobs", "credits", "providerAttempts", "mediaArtifacts"],
      "additionalProperties": false,
      "properties": {
        "generatedAt": { "type": "string", "format": "date-time" },
        "projects": {
          "type": "object",
          "required": ["total", "active", "done", "failed"],
          "additionalProperties": false,
          "properties": {
            "total": { "type": "integer", "minimum": 0 },
            "active": { "type": "integer", "minimum": 0 },
            "done": { "type": "integer", "minimum": 0 },
            "failed": { "type": "integer", "minimum": 0 }
          }
        },
        "jobs": {
          "type": "object",
          "required": ["generation", "image", "render"],
          "additionalProperties": false,
          "properties": {
            "generation": { "$ref": "#/$defs/JobStatusCounts" },
            "image": { "$ref": "#/$defs/JobStatusCounts" },
            "render": { "$ref": "#/$defs/JobStatusCounts" }
          }
        },
        "credits": {
          "type": "object",
          "required": ["balance", "spent", "reserved", "available", "captured", "refunded"],
          "additionalProperties": false,
          "properties": {
            "balance": { "type": "number", "minimum": 0 },
            "spent": { "type": "number", "minimum": 0 },
            "reserved": { "type": "number", "minimum": 0 },
            "available": { "type": "number", "minimum": 0 },
            "captured": { "type": "number", "minimum": 0 },
            "refunded": { "type": "number", "minimum": 0 }
          }
        },
        "providerAttempts": {
          "type": "object",
          "required": ["total", "succeeded", "failed", "cancelled", "retryableFailures", "fallbackSuggested", "avgLatencyMs"],
          "additionalProperties": false,
          "properties": {
            "total": { "type": "integer", "minimum": 0 },
            "succeeded": { "type": "integer", "minimum": 0 },
            "failed": { "type": "integer", "minimum": 0 },
            "cancelled": { "type": "integer", "minimum": 0 },
            "retryableFailures": { "type": "integer", "minimum": 0 },
            "fallbackSuggested": { "type": "integer", "minimum": 0 },
            "avgLatencyMs": { "type": ["number", "null"], "minimum": 0 }
          }
        },
        "mediaArtifacts": {
          "type": "object",
          "required": ["total", "images", "videos", "external"],
          "additionalProperties": false,
          "properties": {
            "total": { "type": "integer", "minimum": 0 },
            "images": { "type": "integer", "minimum": 0 },
            "videos": { "type": "integer", "minimum": 0 },
            "external": { "type": "integer", "minimum": 0 }
          }
        }
      }
    },
    "Scene": {
      "type": "object",
      "required": ["id", "projectId", "order", "title"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^scn_" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "order": { "type": "integer", "minimum": 0 },
        "title": { "type": "string" },
        "setting": { "type": "string" },
        "timeOfDay": { "type": "string" }
      }
    },
    "Shot": {
      "type": "object",
      "required": [
        "id",
        "projectId",
        "sceneId",
        "order",
        "title",
        "durationSec",
        "saec",
        "requirements",
        "status",
        "selectedTakeId",
        "qualityFlags",
        "referenceImageIds",
        "directionSpec"
      ],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^sht_" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "sceneId": { "type": "string", "pattern": "^scn_" },
        "order": { "type": "integer", "minimum": 0 },
        "title": { "type": "string" },
        "durationSec": { "type": "number", "minimum": 1, "maximum": 16 },
        "saec": { "$ref": "#/$defs/SAEC" },
        "requirements": { "$ref": "#/$defs/ShotRequirements" },
        "status": { "$ref": "#/$defs/ShotStatus" },
        "selectedTakeId": { "type": ["string", "null"], "pattern": "^tak_" },
        "qualityFlags": {
          "type": "array",
          "items": { "$ref": "#/$defs/QualityFlag" }
        },
        "referenceImageIds": {
          "type": "array",
          "items": { "type": "string", "pattern": "^img_" }
        },
        "directionSpec": { "$ref": "#/$defs/DirectionSpec" }
      }
    },
    "DirectionSpec": {
      "type": "object",
      "required": ["camera", "composition", "lighting", "motion", "style", "avoid", "notes"],
      "additionalProperties": false,
      "properties": {
        "camera": { "type": "string" },
        "composition": { "type": "string" },
        "lighting": { "type": "string" },
        "motion": { "type": "string" },
        "style": { "type": "string" },
        "avoid": {
          "type": "array",
          "items": { "type": "string" }
        },
        "notes": { "type": "string" }
      }
    },
    "Take": {
      "type": "object",
      "required": ["id", "shotId", "label", "status", "durationSec", "tier", "createdAt"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^tak_" },
        "shotId": { "type": "string", "pattern": "^sht_" },
        "label": { "type": "string" },
        "status": { "$ref": "#/$defs/JobStatus" },
        "videoUrl": { "type": ["string", "null"], "format": "uri" },
        "posterUrl": { "type": ["string", "null"], "format": "uri" },
        "durationSec": { "type": "number", "minimum": 1 },
        "tier": { "$ref": "#/$defs/Tier" },
        "engineUsed": { "type": ["string", "null"], "description": "Internal/debug only; never render in user UI." },
        "metrics": { "$ref": "#/$defs/TakeMetrics" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "TakeMetrics": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "fidelity": { "type": "number", "minimum": 1, "maximum": 5 },
        "consistency": { "type": "number", "minimum": 1, "maximum": 5 },
        "motion": { "type": "number", "minimum": 1, "maximum": 5 },
        "transition": { "type": "number", "minimum": 1, "maximum": 5 },
        "audio": { "type": "number", "minimum": 1, "maximum": 5 },
        "completeness": { "type": "number", "minimum": 1, "maximum": 5 },
        "overall": { "type": "number", "minimum": 1, "maximum": 5 }
      }
    },
    "ProviderAttemptStatus": {
      "type": "string",
      "enum": ["queued", "submitted", "polling", "succeeded", "failed", "cancelled"]
    },
    "ProviderAttempt": {
      "type": "object",
      "required": ["id", "provider", "model", "requestId", "status", "startedAt", "completedAt", "latencyMs", "errorCode", "retryable", "fallbackSuggested"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^pat_" },
        "provider": { "type": "string" },
        "model": { "type": "string" },
        "requestId": { "type": ["string", "null"] },
        "status": { "$ref": "#/$defs/ProviderAttemptStatus" },
        "startedAt": { "type": "string", "format": "date-time" },
        "completedAt": { "type": ["string", "null"], "format": "date-time" },
        "latencyMs": { "type": ["number", "null"], "minimum": 0 },
        "errorCode": { "type": ["string", "null"] },
        "retryable": { "type": "boolean" },
        "fallbackSuggested": { "type": "boolean" }
      }
    },
    "GenerationJob": {
      "type": "object",
      "required": ["id", "shotId", "takeId", "projectId", "status", "progress", "stage", "shouldFail", "dueAt", "createdAt", "updatedAt", "error", "promptPackage", "routing", "providerAttempts"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^gen_" },
        "shotId": { "type": "string", "pattern": "^sht_" },
        "takeId": { "type": "string", "pattern": "^tak_" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "status": { "$ref": "#/$defs/JobStatus" },
        "progress": { "type": "number", "minimum": 0, "maximum": 1 },
        "etaSec": { "type": ["integer", "null"], "minimum": 0 },
        "stage": { "type": "string" },
        "shouldFail": { "type": "boolean" },
        "dueAt": { "type": "integer" },
        "error": { "$ref": "#/$defs/ErrorResponse" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "promptPackage": { "$ref": "#/$defs/GenerationPromptPackage" },
        "routing": { "$ref": "#/$defs/ProviderRoutingDecision" },
        "providerAttempts": { "type": "array", "items": { "$ref": "#/$defs/ProviderAttempt" } }
      }
    },
    "ProviderRouteTarget": {
      "type": "object",
      "required": ["provider", "model"],
      "additionalProperties": false,
      "properties": {
        "provider": { "type": "string" },
        "model": { "type": "string" }
      }
    },
    "ProviderHealthStatus": {
      "type": "string",
      "enum": ["healthy", "degraded", "down"]
    },
    "ProviderHealthTarget": {
      "type": "object",
      "required": ["provider", "model", "status", "reason", "checkedAt", "input", "supportsAudio"],
      "additionalProperties": false,
      "properties": {
        "provider": { "type": "string" },
        "model": { "type": "string" },
        "status": { "$ref": "#/$defs/ProviderHealthStatus" },
        "reason": { "type": ["string", "null"] },
        "checkedAt": { "type": ["string", "null"], "format": "date-time" },
        "input": { "type": "array", "items": { "type": "string" } },
        "supportsAudio": { "type": ["boolean", "string", "null"] }
      }
    },
    "ProviderHealthSnapshot": {
      "type": "object",
      "required": ["generatedAt", "summary", "targets"],
      "additionalProperties": false,
      "properties": {
        "generatedAt": { "type": "string", "format": "date-time" },
        "summary": {
          "type": "object",
          "required": ["total", "healthy", "degraded", "down"],
          "additionalProperties": false,
          "properties": {
            "total": { "type": "integer", "minimum": 0 },
            "healthy": { "type": "integer", "minimum": 0 },
            "degraded": { "type": "integer", "minimum": 0 },
            "down": { "type": "integer", "minimum": 0 }
          }
        },
        "targets": { "type": "array", "items": { "$ref": "#/$defs/ProviderHealthTarget" } }
      }
    },
    "ProviderRoutingDecision": {
      "type": "object",
      "required": ["ruleId", "selected", "candidates", "rejected", "splitTakeIndex", "fallbackEnabled", "hiddenFromUser"],
      "additionalProperties": false,
      "properties": {
        "ruleId": { "type": "string" },
        "selected": { "$ref": "#/$defs/ProviderRouteTarget" },
        "candidates": {
          "type": "array",
          "items": { "$ref": "#/$defs/ProviderRouteTarget" }
        },
        "rejected": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["provider", "model", "reason"],
            "additionalProperties": false,
            "properties": {
              "provider": { "type": "string" },
              "model": { "type": "string" },
              "reason": { "type": "string" }
            }
          }
        },
        "splitTakeIndex": { "type": "integer", "minimum": 0 },
        "fallbackEnabled": { "type": "boolean" },
        "hiddenFromUser": { "type": "boolean" }
      }
    },
    "ProviderInvocationInputKind": {
      "type": "string",
      "enum": ["text", "image", "first_last_frames"]
    },
    "ProviderInvocation": {
      "type": "object",
      "required": ["jobId", "takeId", "projectId", "shotId", "provider", "model", "routingRuleId", "inputKind", "request", "policy", "responseContract"],
      "additionalProperties": false,
      "properties": {
        "jobId": { "type": "string", "pattern": "^gen_" },
        "takeId": { "type": "string", "pattern": "^tak_" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "shotId": { "type": "string", "pattern": "^sht_" },
        "provider": { "type": "string" },
        "model": { "type": "string" },
        "routingRuleId": { "type": "string" },
        "inputKind": { "$ref": "#/$defs/ProviderInvocationInputKind" },
        "request": {
          "type": "object",
          "required": ["prompt", "negativePrompt", "aspect", "durationSec", "tier", "references", "startFrameUrl", "lastFrameUrl"],
          "additionalProperties": false,
          "properties": {
            "prompt": { "type": "string" },
            "negativePrompt": { "type": "string" },
            "aspect": { "$ref": "#/$defs/Aspect" },
            "durationSec": { "type": "integer", "minimum": 1 },
            "tier": { "$ref": "#/$defs/Tier" },
            "references": { "type": "array", "items": { "$ref": "#/$defs/GenerationReference" } },
            "startFrameUrl": { "type": ["string", "null"] },
            "lastFrameUrl": { "type": ["string", "null"] }
          }
        },
        "policy": {
          "type": "object",
          "required": ["hiddenFromUser", "fallbackEnabled", "rightsReviewRequired", "storageIngestRequired"],
          "additionalProperties": false,
          "properties": {
            "hiddenFromUser": { "type": "boolean" },
            "fallbackEnabled": { "type": "boolean" },
            "rightsReviewRequired": { "type": "boolean" },
            "storageIngestRequired": { "type": "boolean" }
          }
        },
        "responseContract": {
          "type": "object",
          "required": ["expectedKind", "outputRole", "ingest", "progress"],
          "additionalProperties": false,
          "properties": {
            "expectedKind": { "type": "string", "const": "video" },
            "outputRole": { "type": "string", "const": "take_video" },
            "ingest": { "type": "string", "const": "copy_to_storage" },
            "progress": { "type": "string", "const": "async_polling" }
          }
        }
      }
    },
    "GenerationReference": {
      "type": "object",
      "required": ["assetId", "role", "mode", "url", "rightsStatus"],
      "additionalProperties": false,
      "properties": {
        "assetId": { "type": "string", "pattern": "^img_" },
        "role": { "$ref": "#/$defs/ImageAssetRole" },
        "mode": { "$ref": "#/$defs/AssetUsageMode" },
        "url": { "type": "string" },
        "rightsStatus": { "type": "string", "enum": ["user_confirmed", "generated", "needs_review"] }
      }
    },
    "GenerationPromptPackage": {
      "type": "object",
      "required": ["projectId", "shotId", "durationSec", "saec", "directionSpec", "requirements", "references", "routingHints"],
      "additionalProperties": false,
      "properties": {
        "projectId": { "type": "string", "pattern": "^prj_" },
        "shotId": { "type": "string", "pattern": "^sht_" },
        "durationSec": { "type": "integer", "minimum": 1 },
        "saec": { "$ref": "#/$defs/SAEC" },
        "directionSpec": { "$ref": "#/$defs/DirectionSpec" },
        "requirements": { "$ref": "#/$defs/ShotRequirements" },
        "references": {
          "type": "array",
          "items": { "$ref": "#/$defs/GenerationReference" }
        },
        "routingHints": {
          "type": "object",
          "required": [
            "startFrameAssetId",
            "lastFrameAssetId",
            "styleReferenceAssetIds",
            "characterReferenceAssetIds",
            "productReferenceAssetIds",
            "backgroundReferenceAssetIds",
            "rightsReviewRequired"
          ],
          "additionalProperties": false,
          "properties": {
            "startFrameAssetId": { "type": ["string", "null"], "pattern": "^img_" },
            "lastFrameAssetId": { "type": ["string", "null"], "pattern": "^img_" },
            "styleReferenceAssetIds": { "type": "array", "items": { "type": "string", "pattern": "^img_" } },
            "characterReferenceAssetIds": { "type": "array", "items": { "type": "string", "pattern": "^img_" } },
            "productReferenceAssetIds": { "type": "array", "items": { "type": "string", "pattern": "^img_" } },
            "backgroundReferenceAssetIds": { "type": "array", "items": { "type": "string", "pattern": "^img_" } },
            "rightsReviewRequired": { "type": "boolean" }
          }
        }
      }
    },
    "ImageAsset": {
      "type": "object",
      "required": ["id", "projectId", "kind", "role", "source", "label", "prompt", "url", "thumbUrl", "aspect", "width", "height", "rights", "createdAt", "updatedAt"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^img_" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "kind": { "type": "string", "const": "image" },
        "role": { "$ref": "#/$defs/ImageAssetRole" },
        "source": { "type": "string", "enum": ["image_maker", "upload", "external"] },
        "label": { "type": "string" },
        "prompt": { "type": "string" },
        "url": { "type": "string" },
        "thumbUrl": { "type": "string" },
        "aspect": { "$ref": "#/$defs/Aspect" },
        "width": { "type": "integer", "minimum": 1 },
        "height": { "type": "integer", "minimum": 1 },
        "rights": {
          "type": "object",
          "required": ["status", "note"],
          "additionalProperties": false,
          "properties": {
            "status": { "type": "string", "enum": ["user_confirmed", "generated", "needs_review"] },
            "note": { "type": "string" }
          }
        },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      }
    },
    "ImageVariant": {
      "type": "object",
      "required": ["id", "assetId", "label", "status", "url", "thumbUrl", "scoreLabel"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^ivar_" },
        "assetId": { "type": ["string", "null"], "pattern": "^img_" },
        "label": { "type": "string" },
        "status": { "$ref": "#/$defs/JobStatus" },
        "url": { "type": ["string", "null"] },
        "thumbUrl": { "type": ["string", "null"] },
        "scoreLabel": { "type": "string", "enum": ["추천", "안정적", "확인 필요"] }
      }
    },
    "ImageJob": {
      "type": "object",
      "required": ["id", "projectId", "status", "progress", "etaSec", "stage", "prompt", "purpose", "role", "aspect", "style", "count", "variants", "dueAt", "createdAt", "updatedAt", "error"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^ijob_" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "status": { "$ref": "#/$defs/JobStatus" },
        "progress": { "type": "number", "minimum": 0, "maximum": 1 },
        "etaSec": { "type": ["integer", "null"], "minimum": 0 },
        "stage": { "type": "string", "enum": ["queued", "prompting", "generating", "saving", "done", "failed"] },
        "prompt": { "type": "string" },
        "purpose": { "$ref": "#/$defs/ImageMakerPurpose" },
        "role": { "$ref": "#/$defs/ImageAssetRole" },
        "aspect": { "$ref": "#/$defs/Aspect" },
        "style": { "type": "string" },
        "count": { "type": "integer", "minimum": 1, "maximum": 4 },
        "variants": { "type": "array", "items": { "$ref": "#/$defs/ImageVariant" } },
        "dueAt": { "type": "integer" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "error": { "$ref": "#/$defs/ErrorResponse" }
      }
    },
    "ImageWorkerInvocation": {
      "type": "object",
      "required": ["jobId", "projectId", "request", "outputs", "policy", "responseContract"],
      "additionalProperties": false,
      "properties": {
        "jobId": { "type": "string", "pattern": "^ijob_" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "request": {
          "type": "object",
          "required": ["prompt", "purpose", "role", "aspect", "style", "count"],
          "additionalProperties": false,
          "properties": {
            "prompt": { "type": "string" },
            "purpose": { "$ref": "#/$defs/ImageMakerPurpose" },
            "role": { "$ref": "#/$defs/ImageAssetRole" },
            "aspect": { "$ref": "#/$defs/Aspect" },
            "style": { "type": "string" },
            "count": { "type": "integer", "minimum": 1, "maximum": 4 }
          }
        },
        "outputs": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["variantId", "label", "scoreLabel", "imageStorageKey", "thumbnailStorageKey"],
            "additionalProperties": false,
            "properties": {
              "variantId": { "type": "string", "pattern": "^ivar_" },
              "label": { "type": "string" },
              "scoreLabel": { "type": "string", "enum": ["추천", "안정적", "확인 필요"] },
              "imageStorageKey": { "type": "string" },
              "thumbnailStorageKey": { "type": "string" }
            }
          }
        },
        "policy": {
          "type": "object",
          "required": ["rightsStatus", "registerAsAssets", "storageIngestRequired"],
          "additionalProperties": false,
          "properties": {
            "rightsStatus": { "type": "string", "const": "generated" },
            "registerAsAssets": { "type": "boolean" },
            "storageIngestRequired": { "type": "boolean" }
          }
        },
        "responseContract": {
          "type": "object",
          "required": ["expectedKind", "outputRole", "thumbnailRole", "ingest", "progress"],
          "additionalProperties": false,
          "properties": {
            "expectedKind": { "type": "string", "const": "image" },
            "outputRole": { "type": "string", "const": "image_asset" },
            "thumbnailRole": { "type": "string", "const": "image_thumbnail" },
            "ingest": { "type": "string", "const": "copy_to_storage" },
            "progress": { "type": "string", "const": "async_polling" }
          }
        }
      }
    },
    "AssetUsage": {
      "type": "object",
      "required": ["assetId", "role", "target", "targetId", "mode", "createdAt"],
      "additionalProperties": false,
      "properties": {
        "assetId": { "type": "string", "pattern": "^img_" },
        "role": { "$ref": "#/$defs/ImageAssetRole" },
        "target": { "type": "string", "enum": ["project", "shot"] },
        "targetId": { "type": "string" },
        "mode": { "$ref": "#/$defs/AssetUsageMode" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "ReferenceBoard": {
      "type": "object",
      "required": ["projectId", "productImages", "characterImages", "locationImages", "styleImages", "keyframes", "thumbnails", "logos", "backgrounds", "usages"],
      "additionalProperties": false,
      "properties": {
        "projectId": { "type": "string", "pattern": "^prj_" },
        "productImages": { "type": "array", "items": { "type": "string", "pattern": "^img_" } },
        "characterImages": { "type": "array", "items": { "type": "string", "pattern": "^img_" } },
        "locationImages": { "type": "array", "items": { "type": "string", "pattern": "^img_" } },
        "styleImages": { "type": "array", "items": { "type": "string", "pattern": "^img_" } },
        "keyframes": { "type": "array", "items": { "type": "string", "pattern": "^img_" } },
        "thumbnails": { "type": "array", "items": { "type": "string", "pattern": "^img_" } },
        "logos": { "type": "array", "items": { "type": "string", "pattern": "^img_" } },
        "backgrounds": { "type": "array", "items": { "type": "string", "pattern": "^img_" } },
        "usages": { "type": "array", "items": { "$ref": "#/$defs/AssetUsage" } }
      }
    },
    "ProjectBundle": {
      "type": "object",
      "required": [
        "project",
        "scenes",
        "shots",
        "takes",
        "generationJobs",
        "renderJobs",
        "imageAssets",
        "imageJobs",
        "referenceBoard",
        "editState",
        "credits",
        "creditTransactions",
        "mediaArtifacts",
        "renderSourceHash"
      ],
      "additionalProperties": false,
      "properties": {
        "project": { "$ref": "#/$defs/Project" },
        "scenes": { "type": "array", "items": { "$ref": "#/$defs/Scene" } },
        "shots": { "type": "array", "items": { "$ref": "#/$defs/Shot" } },
        "takes": { "type": "array", "items": { "$ref": "#/$defs/Take" } },
        "generationJobs": { "type": "array", "items": { "$ref": "#/$defs/GenerationJob" } },
        "renderJobs": { "type": "array", "items": { "$ref": "#/$defs/RenderJob" } },
        "imageAssets": { "type": "array", "items": { "$ref": "#/$defs/ImageAsset" } },
        "imageJobs": { "type": "array", "items": { "$ref": "#/$defs/ImageJob" } },
        "referenceBoard": { "$ref": "#/$defs/ReferenceBoard" },
        "editState": { "$ref": "#/$defs/EditState" },
        "credits": { "$ref": "#/$defs/StudioCredits" },
        "creditTransactions": { "type": "array", "items": { "$ref": "#/$defs/CreditTransaction" } },
        "mediaArtifacts": { "type": "array", "items": { "$ref": "#/$defs/MediaArtifact" } },
        "renderSourceHash": { "$ref": "#/$defs/RenderSourceHash" }
      }
    },
    "StoryboardUpdateInput": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "scenes": {
          "type": "array",
          "items": { "$ref": "#/$defs/Scene" }
        },
        "shots": {
          "type": "array",
          "items": { "$ref": "#/$defs/Shot" }
        }
      }
    },
    "RenderJob": {
      "type": "object",
      "required": [
        "id",
        "projectId",
        "spec",
        "stage",
        "progress",
        "status",
        "outputUrl",
        "shareUrl",
        "etaSec",
        "dueAt",
        "createdAt",
        "updatedAt",
        "error",
        "rightsReview",
        "renderPlan"
      ],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^rnd_" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "spec": { "$ref": "#/$defs/ExportSpec" },
        "stage": {
          "type": "string",
          "enum": ["assemble", "audio_mix", "caption_burn", "encode", "upscale", "done"]
        },
        "progress": { "type": "number", "minimum": 0, "maximum": 1 },
        "status": { "$ref": "#/$defs/JobStatus" },
        "outputUrl": { "type": ["string", "null"], "format": "uri" },
        "shareUrl": { "type": ["string", "null"], "format": "uri" },
        "etaSec": { "type": ["integer", "null"], "minimum": 0 },
        "dueAt": { "type": "integer" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "error": { "$ref": "#/$defs/ErrorResponse" },
        "rightsReview": { "$ref": "#/$defs/RenderRightsReview" },
        "renderPlan": { "$ref": "#/$defs/RenderPlan" }
      }
    },
    "RenderPreview": {
      "type": "object",
      "required": ["projectId", "spec", "sourceHash", "rightsReview", "renderPlan", "estimate"],
      "additionalProperties": false,
      "properties": {
        "projectId": { "type": "string", "pattern": "^prj_" },
        "spec": { "$ref": "#/$defs/ExportSpec" },
        "sourceHash": { "$ref": "#/$defs/RenderSourceHash" },
        "rightsReview": { "$ref": "#/$defs/RenderRightsReview" },
        "renderPlan": { "$ref": "#/$defs/RenderPlan" },
        "estimate": { "$ref": "#/$defs/CostEstimate" }
      }
    },
    "RenderPlan": {
      "type": "object",
      "required": ["projectId", "spec", "sourceHash", "totalDurationSec", "missingShotIds", "shots", "edit"],
      "additionalProperties": false,
      "properties": {
        "projectId": { "type": "string", "pattern": "^prj_" },
        "spec": { "$ref": "#/$defs/ExportSpec" },
        "sourceHash": { "$ref": "#/$defs/RenderSourceHash" },
        "totalDurationSec": { "type": "number", "minimum": 0 },
        "missingShotIds": {
          "type": "array",
          "items": { "type": "string", "pattern": "^sht_" }
        },
        "shots": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["shotId", "takeId", "order", "title", "durationSec", "videoUrl", "posterUrl", "tier"],
            "additionalProperties": false,
            "properties": {
              "shotId": { "type": "string", "pattern": "^sht_" },
              "takeId": { "type": "string", "pattern": "^tak_" },
              "order": { "type": "integer", "minimum": 0 },
              "title": { "type": "string" },
              "durationSec": { "type": "number", "minimum": 1 },
              "videoUrl": { "type": ["string", "null"] },
              "posterUrl": { "type": ["string", "null"] },
              "tier": { "$ref": "#/$defs/Tier" }
            }
          }
        },
        "edit": { "$ref": "#/$defs/EditState" }
      }
    },
    "RenderWorkerInvocation": {
      "type": "object",
      "required": ["jobId", "projectId", "sourceHash", "spec", "inputs", "missingShotIds", "edit", "output", "policy"],
      "additionalProperties": false,
      "properties": {
        "jobId": { "type": "string", "pattern": "^rnd_" },
        "projectId": { "type": "string", "pattern": "^prj_" },
        "sourceHash": { "$ref": "#/$defs/RenderSourceHash" },
        "spec": { "$ref": "#/$defs/ExportSpec" },
        "inputs": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["shotId", "takeId", "order", "title", "durationSec", "videoUrl", "posterUrl"],
            "additionalProperties": false,
            "properties": {
              "shotId": { "type": "string", "pattern": "^sht_" },
              "takeId": { "type": "string", "pattern": "^tak_" },
              "order": { "type": "integer", "minimum": 0 },
              "title": { "type": "string" },
              "durationSec": { "type": "number", "minimum": 1 },
              "videoUrl": { "type": "string" },
              "posterUrl": { "type": ["string", "null"] }
            }
          }
        },
        "missingShotIds": { "type": "array", "items": { "type": "string", "pattern": "^sht_" } },
        "edit": { "$ref": "#/$defs/EditState" },
        "output": {
          "type": "object",
          "required": ["role", "container", "storageKey", "shareUrlRequired"],
          "additionalProperties": false,
          "properties": {
            "role": { "type": "string", "const": "render_output" },
            "container": { "type": "string", "const": "mp4" },
            "storageKey": { "type": "string" },
            "shareUrlRequired": { "type": "boolean" }
          }
        },
        "policy": {
          "type": "object",
          "required": ["missingShotPolicy", "burnCaptions", "emitSrt", "audioMix", "voiceover", "transitions"],
          "additionalProperties": false,
          "properties": {
            "missingShotPolicy": { "type": "string", "const": "skip_with_notice" },
            "burnCaptions": { "type": "boolean" },
            "emitSrt": { "type": "boolean" },
            "audioMix": { "type": "boolean" },
            "voiceover": { "type": "boolean" },
            "transitions": { "type": "string", "enum": ["none", "soft"] }
          }
        }
      }
    },
    "EditState": {
      "type": "object",
      "required": ["projectId", "captions", "bgm", "voiceover", "transitions", "commands"],
      "additionalProperties": false,
      "properties": {
        "projectId": { "type": "string", "pattern": "^prj_" },
        "captions": {
          "type": "object",
          "required": ["enabled", "mode", "source"],
          "additionalProperties": false,
          "properties": {
            "enabled": { "type": "boolean" },
            "mode": { "type": "string", "enum": ["burn-in", "srt", "both"] },
            "source": { "type": "string", "enum": ["script-first", "stt"] }
          }
        },
        "bgm": {
          "type": "object",
          "required": ["enabled", "track", "ducking"],
          "additionalProperties": false,
          "properties": {
            "enabled": { "type": "boolean" },
            "track": { "type": "string" },
            "ducking": { "type": "boolean" }
          }
        },
        "voiceover": {
          "type": "object",
          "required": ["enabled", "voice", "source"],
          "additionalProperties": false,
          "properties": {
            "enabled": { "type": "boolean" },
            "voice": { "type": "string" },
            "source": { "type": "string", "enum": ["licensed_tts", "user_upload"] }
          }
        },
        "transitions": { "type": "string", "enum": ["none", "soft"] },
        "commands": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["command", "createdAt"],
            "additionalProperties": false,
            "properties": {
              "command": { "type": "string" },
              "createdAt": { "type": "string", "format": "date-time" }
            }
          }
        }
      }
    },
    "EditCommandInput": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "command": { "type": "string" }
      }
    },
    "EditAudioPatch": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "captions": { "$ref": "#/$defs/EditState/properties/captions" },
        "bgm": { "$ref": "#/$defs/EditState/properties/bgm" },
        "voiceover": { "$ref": "#/$defs/EditState/properties/voiceover" },
        "transitions": { "$ref": "#/$defs/EditState/properties/transitions" }
      }
    },
    "RenderRightsReview": {
      "type": "object",
      "required": ["required", "assetIds", "items"],
      "additionalProperties": false,
      "properties": {
        "required": { "type": "boolean" },
        "assetIds": {
          "type": "array",
          "items": { "type": "string", "pattern": "^img_" }
        },
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["assetId", "label", "role", "rightsStatus", "note", "targetShotIds"],
            "additionalProperties": false,
            "properties": {
              "assetId": { "type": "string", "pattern": "^img_" },
              "label": { "type": "string" },
              "role": { "$ref": "#/$defs/ImageAssetRole" },
              "rightsStatus": { "type": "string", "enum": ["user_confirmed", "generated", "needs_review"] },
              "note": { "type": "string" },
              "targetShotIds": {
                "type": "array",
                "items": { "type": "string", "pattern": "^sht_" }
              }
            }
          }
        }
      }
    },
    "AssetDeleteResult": {
      "type": "object",
      "required": ["deleted", "assetId", "blockedByUsage", "usageCount", "remainingAssets"],
      "additionalProperties": false,
      "properties": {
        "deleted": { "type": "boolean" },
        "assetId": { "type": "string", "pattern": "^img_" },
        "blockedByUsage": { "type": "boolean" },
        "usageCount": { "type": "integer", "minimum": 0 },
        "remainingAssets": { "type": "integer", "minimum": 0 }
      }
    },
    "ExportSpec": {
      "type": "object",
      "required": ["resolution", "cut", "aspect", "caption"],
      "additionalProperties": false,
      "properties": {
        "resolution": { "type": "string", "enum": ["720p", "1080p", "4k"] },
        "cut": { "type": "string", "enum": ["6s", "15s", "30s", "full"] },
        "aspect": { "$ref": "#/$defs/Aspect" },
        "caption": { "type": "string", "enum": ["none", "burn-in", "srt", "both"] }
      }
    },
    "CostEstimate": {
      "type": "object",
      "required": ["credits", "etaSec"],
      "additionalProperties": false,
      "properties": {
        "credits": { "type": "number", "minimum": 0 },
        "etaSec": { "type": "integer", "minimum": 0 }
      }
    },
    "ErrorResponse": {
      "type": ["object", "null"],
      "required": ["code", "userMessage", "retryable", "fallbackSuggested"],
      "additionalProperties": false,
      "properties": {
        "code": { "type": "string" },
        "userMessage": { "type": "string" },
        "retryable": { "type": "boolean" },
        "fallbackSuggested": { "type": "boolean" }
      }
    }
  },
  "properties": {
    "project": { "$ref": "#/$defs/Project" },
    "projectBundle": { "$ref": "#/$defs/ProjectBundle" },
    "creditTransaction": { "$ref": "#/$defs/CreditTransaction" },
    "mediaArtifact": { "$ref": "#/$defs/MediaArtifact" },
    "mediaArtifactInventory": { "$ref": "#/$defs/MediaArtifactInventory" },
    "storageCleanupPlan": { "$ref": "#/$defs/StorageCleanupPlan" },
    "cancelJobResult": { "$ref": "#/$defs/CancelJobResult" },
    "jobQueueSnapshot": { "$ref": "#/$defs/JobQueueSnapshot" },
    "workerDispatchSnapshot": { "$ref": "#/$defs/WorkerDispatchSnapshot" },
    "workerLeaseSnapshot": { "$ref": "#/$defs/WorkerLeaseSnapshot" },
    "workerLeaseRenewResult": { "$ref": "#/$defs/WorkerLeaseRenewResult" },
    "workerCompletionSnapshot": { "$ref": "#/$defs/WorkerCompletionSnapshot" },
    "runtimeReadiness": { "$ref": "#/$defs/RuntimeReadiness" },
    "systemMetrics": { "$ref": "#/$defs/SystemMetrics" },
    "scene": { "$ref": "#/$defs/Scene" },
    "shot": { "$ref": "#/$defs/Shot" },
    "take": { "$ref": "#/$defs/Take" },
    "generationJob": { "$ref": "#/$defs/GenerationJob" },
    "providerHealthSnapshot": { "$ref": "#/$defs/ProviderHealthSnapshot" },
    "imageWorkerInvocation": { "$ref": "#/$defs/ImageWorkerInvocation" },
    "providerInvocation": { "$ref": "#/$defs/ProviderInvocation" },
    "renderWorkerInvocation": { "$ref": "#/$defs/RenderWorkerInvocation" },
    "renderJob": { "$ref": "#/$defs/RenderJob" }
  }
}
