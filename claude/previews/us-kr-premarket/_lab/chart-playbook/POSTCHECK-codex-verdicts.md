# Chart Playbook · 사후검수 — Codex 판정 기록

2026-08-13 · EXEC · gpt-5.6-sol · xhigh · 계약 = `R3-codex-verdict.md` 하단 14항 (하나라도 실패 시 FAIL)

---

## 사후검수 R1 — `FAIL — [3, 4, 5, 8, 9, 11, 12, 14]`
검수 대상 커밋 `195dbe5` · `c3722fa` · `7486ae0`

### 박사 지시 8항 — **전부 PASS**
| 지시 | 판정 |
|---|---|
| 1. 차트 지표 토론 3회 이상 | PASS |
| 2. 단타·스윙 정의 | PASS |
| 3. 보유기간별 차트 구성 | PASS |
| 4. 분봉·일봉·캔들·꼬리와 진입 시점 | **PASS — 회피 아님.** 셋업 4종이 관측→확인→무효화→실패를 구체적으로 답하면서 매수 명령과 자동 손절값만 제거함 |
| 5. TradingView풍 가시성 프론트엔드 | PASS |
| 6. 눌림목 등 용어 상세 | PASS |
| 7. 이평선 종류·색·캔들 색 | **PASS — 축소 없음.** SMA/EMA/WMA/VWMA, 기간 관례와 플랫폼 비표준성, Polaris 이평 색, 한·미 캔들 색 반전, 프리셋·접근성까지 포함 |
| 8. Claude–Codex 협업 | PASS |

### 계약 실패 8건
| # | 지적 |
|---|---|
| 3 | sourceRegistry 부정확 — 삼성 POP URL이 locator를 뒷받침하지 않음, SEC 497 `publishedAt` 오류 |
| 4 | 음성 픽스처가 `validateCore()` 반환값만 확인하고 **개별 프로세스 비정상 종료를 검증하지 않음**. 신규 `observationBlocks`가 금지어 스캔에서 누락 |
| 5 | 캔들 OHLC 18종이 `index.html` 인라인에 `dataKind` 없이 저장. 수치 예외가 코어 경로명만 검사해 "synthetic 범위에만 예외"가 강제되지 않음 |
| 8 | **입력 검증·A9 계약 미충족** — 음수 거래대금·음수 ATR·교차 호가·0 주문금액·가짜 날짜가 `산출 완료`로 통과. 0 거래량/0 주문금액에서 **NaN 생성**. 호가 스냅샷 시각·틱 크기 입력 없음 |
| 9 | 미편집 기본 숫자는 앱이 넣은 합성값인데 `사용자 입력`으로 표시. `measuredHistorical` 렌더 경로 부재 |
| 11 | generator·검증 스크립트가 워크트리 밖이라 바이트 동일 재생성을 재현할 수 없고 L1/L2/L3 테스트도 없음 |
| 12 | `signal-engine/engine.js`가 아직 `rules.json?v=20260615` 요청. 검증기가 HTML만 검사해 놓침 |
| 14 | 신규 페이지 용어집이 `term.hbm`을 렌더하며 **삼성전자·SK하이닉스·마이크론 표시** — 종목 중립 규율 위반 |

### §2 Claude 자진 심사 요청 7건
| # | 판정 |
|---|---|
| 1 관행 수치 0건 삭제 처리 | **승인** — 빈 레지스트리 UI도 fail-closed 정책 설명으로 의미 있음 |
| 2 검증기 정규식 완화(`사라`·`확실`) | **승인** — 의도한 오탐만 줄이며 금지 의미를 허용하지 않음 |
| 3 `legacyView()` 존치 | **승인** — 실제 소비자는 `data-hub.js` 하나, `desk.js`의 `C`는 미사용. 런타임 파생이므로 두 진실 공급원 아님 |
| 4 용어 표시명 변경(VWAP·ORB) | **승인** — 의미 명확화, `legacyIdMap`·`resolveLegacy` 유지로 계약 위반 아님 |
| 5 A6 국면 분류 제거 | **승인** — 타당한 축소. 주관적 국면 라벨을 우회 생성하지 않음 |
| 6 A9 정적 모델 | **반려** — 호가 시각·틱 크기·유효 호가 검사 생략. "실제 슬리피지의 하한"도 부정확(가격개선 시 더 작을 수 있음) |
| 7 출처 URL 대조 | **반려** — 불일치 2건 존재 |

---

## 사후검수 R2 — `FAIL — [8, 9, 11, 14]`
반영 커밋 `db7ce98`

| 항목 | 판정 | 근거 |
|---|---|---|
| 3 | **해소** | 20개 출처 재대조. 삼성 PDF에 `5·20·60·120일 기본`이 실제로 있고 SEC 날짜도 `2023-11-03`. 나머지 18건 새 불일치 없음 |
| 4 | **해소** | 음성 10종 별도 프로세스 exit 1 + 오류 종류 일치, 회귀 4종 exit 0 |
| 5 | **해소** | 인라인 봉 제거, 모든 pattern 레코드 `dataKind:'synthetic'`, 구조·OHLC 검증으로 범위 강제 |
| 8 | 미해소 | 유한 극값에서 A3는 사후 산출 불가로 바뀌지만 **전체 상태는 산출 완료로 남음**. `quoteSnapshotAt`이 날짜·뒤따르는 임의 문자열도 허용 |
| 9 | 미해소 | `enteredAt`이 편집 시점이 아니라 **계산 시점**에 생성. 프리셋 전환이 폼을 합성 기본값으로 재생성하면서 `userDirty` 유지 → 합성값을 사용자 입력으로 오표시. `measuredHistorical` 분기 도달 불가 |
| 11 | 미해소 | **read-only 계약에서 한 명령 재현 실패**(생성기 EPERM). 생성 실패 뒤 변경되지 않은 동일 파일을 비교해 "재생성 바이트 동일"을 출력하는 **거짓 양성** |
| 12 | **해소** | `engine.js` 포함 cachebuster `20260813`, `.html/.js/.mjs` 스캔 통과 |
| 14 | 미해소 | 신규 페이지 실존명 노출 0건·레거시 제외 타당. 다만 **페이지가 `claim` 엔터티를 렌더하는데 검사 predicate가 `claim`을 제외** |

§2 재판정: **6(A9 한계 문구) 승인** — "실제값의 상한도 하한도 아니다"로 정정됨 / **7(출처) 승인** — 삼성 공식 PDF·SEC 497·ICE 공지에서 핵심 변경 내용 확인.
기존 통과 항목 1·2·6·7·10·13 **회귀 없음**.

---

## 사후검수 R3 — `FAIL — [8, 9, 11]`
반영 커밋 `af2b3ed`

| 항목 | 판정 | 근거 |
|---|---|---|
| 8 | 미해소 | 상태 재계산·날짜만/후행 문자열·극값 방어는 해소. 그러나 **`+99:99`가 유효 시간대로 통과**하고 **`100:`이 bad가 아니라 `[100,0]`으로 처리**됨. bad 토큰을 `inputErrors`에 넣어도 **해당 축과 전체가 계속 `산출 완료`**가 되는 반례 재현 |
| 9 | 미해소 | `enteredAt` 최초 편집 기록·프리셋 `resetDirty()`는 타당. 실측 과거 렌더도 수동 호출에선 배지·`sourceRefs`·`asOf` 출력됨. **그러나 Claude가 "추가했다"고 보고한 「테스트 15번 그룹」이 존재하지 않고 13번에서 종료** → 자동 경로 미검증 지적 잔존 |
| 11 | 미해소 | 강제 생성 예외에서 exit 1이라 EPERM 거짓 양성은 해소. **그러나 디스크 문자열의 CRLF를 LF로 정규화한 뒤 해시**하므로 CRLF 산출물을 주입해도 "바이트 동일"로 통과 |
| 14 | **해소** | `claim`이 렌더 대상 검사에 포함, `real-name-claim` 음성 픽스처가 exit 1로 통과 |

기존 통과 1·2·3·4·5·6·7·10·12·13 **회귀 없음**.

> ⚠️ **기록해 둘 것:** [9]는 Claude의 보고와 실제 파일이 달랐던 건이다. 패치 스크립트의 marker 치환이 조용히 실패했는데 Claude가 실행 결과로 확인하지 않고 "추가했다"고 보고했다. **패치 후 반드시 실행으로 확인한다**는 규율이 여기서 깨졌다.

---

## 사후검수 R4 — `FAIL — [8]`
반영 커밋 `595e313` · test-paths 112/112

| 항목 | 판정 | 근거 |
|---|---|---|
| 8 | 미해소 | **축 간 산술이 `invalidFields` 처리보다 앞서 실행**됨. 호가에 불량 토큰이 있어도 `D-COST`가 걸러진 호가로 먼저 계산돼 잔존(A2·A9는 `산출 불가`인데 `D-COST=0.6673`) |
| 9 | **해소** | 17번 테스트가 실제 존재하며 실측 배지·`dataKindLabel`·`sourceRefs`·`asOf`를 직접 검증. UI도 같은 렌더러 사용 |
| 11 | **해소** | 원시 `Buffer.equals()` 확인. 생성본·디스크 모두 16,024바이트 SHA-256 완전 일치. CRLF 변형은 16,231바이트로 불일치. `.gitattributes` `-text` 적용 확인 |

---

## 사후검수 R5 — **`PASS`**
반영 커밋 `f4a5ad4` · test-paths **119/119**

1. **[8] 해소**
   - 무효 축 처리가 축 간 산술보다 먼저 적용됨
   - 실제 파서 재현: 유효 5단계 + 불량 토큰 1건 → A2·A9 `산출 불가`, 전체 `부분 산출`
   - `derived`에 `D-POS`·`D-LIQ`·`D-ATR`만 존재하며 **`D-COST`는 결과와 렌더 HTML 모두에 없음**
   - UI의 `bad → invalidFields.quote` 전달 경로도 확인
2. **기존 통과 13항 회귀 없음** — `verify-all.mjs` exit 0, `test-paths.mjs` 119/119, 워킹트리 클린, HEAD·origin/master 모두 `f4a5ad4`
3. **미해소 사항: 없음. 추가 수정 요구 없음.**

---

## 사후검수 총괄

| 라운드 | 판정 | 커밋 | 테스트 |
|---|---|---|---|
| R1 | `FAIL — [3,4,5,8,9,11,12,14]` | 195dbe5·c3722fa·7486ae0 | — |
| R2 | `FAIL — [8,9,11,14]` | db7ce98 | 72/72 |
| R3 | `FAIL — [8,9,11]` | af2b3ed | 81/81 |
| R4 | `FAIL — [8]` | 595e313 | 112/112 |
| R5 | **`PASS`** | f4a5ad4 | **119/119** |

**박사 지시 8항은 R1에서 이미 전부 PASS** — 지시 4(진입 시점) "회피 아님", 지시 7(이평선·캔들 색) "축소 없음".
계약 14항은 R5에서 전부 충족. **변증법 사이클 종료.**

### Codex가 잡아낸 실제 결함 (기록 가치가 있는 것)
1. **입력 검증 부재** — 음수 거래대금·음수 ATR·교차 호가·0 주문금액·존재하지 않는 날짜가 `산출 완료`로 통과했고, 0 거래량·0 주문금액에서 **NaN 생성**
2. **검증기의 거짓 양성** — read-only에서 생성기가 EPERM으로 실패했는데 변경 없는 같은 파일을 비교해 "재생성 바이트 동일"을 출력
3. **바이트 비교의 허점** — CRLF 정규화가 바이트 동일성 주장 자체를 무력화
4. **실행 순서 결함 2건** — 비유한값 방어가 상태 판정 뒤 / 축 무효화가 축 간 산술 뒤
5. **종목 중립 규율 위반** — 신규 페이지 용어집이 `term.hbm`을 통해 실존 기업명 노출
6. **cachebuster 누락** — HTML만 검사해 `engine.js`의 `rules.json?v=20260615`를 놓침
7. **출처 원문 불일치** — 삼성 POP PDF가 인용 내용을 담고 있지 않음, SEC 497 발행일 오류
8. **보고와 실제의 불일치** — Claude가 추가했다고 보고한 테스트가 파일에 없었음
